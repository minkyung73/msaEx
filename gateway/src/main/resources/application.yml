server:
  port: 8088

---

spring:
  profiles: default
  cloud:
    gateway:
      routes:
        - id: BoundedContext50
          uri: http://localhost:8081
          predicates:
            - Path=/hellos/**, 
        - id: BoundedContext50
          uri: http://localhost:8082
          predicates:
            - Path=/worlds/**, 
        - id: frontend
          uri: http://localhost:8080
          predicates:
            - Path=/**
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "*"
            allowedMethods:
              - "*"
            allowedHeaders:
              - "*"
            allowCredentials: true


---

spring:
  profiles: docker
  cloud:
    gateway:
      routes:
        - id: BoundedContext50
          uri: http://boundedContext50:8080
          predicates:
            - Path=/hellos/**, 
        - id: BoundedContext50
          uri: http://boundedContext50:8080
          predicates:
            - Path=/worlds/**, 
        - id: frontend
          uri: http://frontend:8080
          predicates:
            - Path=/**
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "*"
            allowedMethods:
              - "*"
            allowedHeaders:
              - "*"
            allowCredentials: true

server:
  port: 8080
